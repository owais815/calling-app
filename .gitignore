# mac
.DS_Store

# git folder
.git

# npm
node_modules
npm-debug.log

# package
package-lock.json

# cache
.cache

# personal
# config.js — tracked (LMS customizations; secrets via env vars)
# docker-compose.yml — tracked (production deployment)
docker-push.sh
rec
rtmp